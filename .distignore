# Directories excluded from the WordPress.org distribution build
/.wordpress-org
/.git
/.github
/.cache
/node_modules
/grunt
/vendor
/.revision
/bin
/scripts
/docs

# Linting / static analysis config
.phpcs.xml.dist
phpmd.xml

# Packaging / VCS metadata
.distignore
.gitignore
.removeable-files
Gruntfile.js

# Build & tooling config
webpack.config.js
.wp-env.json

# Lockfiles / generated manifests (no runtime role; /vendor is excluded)
composer.lock
package-lock.json
pnpm-lock.yaml
yarn.lock
package.xml
